# Tasks

## 1. Spec
- [x] 1.1 `tui-shell` delta — correct "eight primary bindings" → "ten" in the
  Hidden bindings requirement.

## 2. Gates
- [ ] 2.1 `openspec validate --specs --strict`,
  `openspec validate fix-footer-binding-count --strict`.
