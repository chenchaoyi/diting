# Tasks — document macos-helper

## 1. Spec backfill — no implementation
- [x] 1.1 Spec extracted from `helper/Sources/wifiscope-helper/main.swift` and `src/wifiscope/_helper.py`; no code changes required.
- [x] 1.2 Cross-checked against the schema-version comments in `main.swift:90-110` (currently 3, with schema-4 BLE additive fields documented in the Wi-Fi-side comment block).

## 2. Optional polish (not blocking archive)
- [x] 2.1 README.md "Permissions" section gains a one-liner pointing to `openspec/specs/macos-helper/spec.md`.
- [x] 2.2 `helper/README.md` cross-links to the canonical spec instead of duplicating the install procedure.
