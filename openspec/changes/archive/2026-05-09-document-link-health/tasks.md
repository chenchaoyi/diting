# Tasks — document link-health

## 1. Spec backfill — no implementation
- [x] 1.1 Spec extracted from `src/wifiscope/latency.py` and the diagnostics rendering in `src/wifiscope/tui.py`.
- [x] 1.2 Cross-checked against the v0.7.0 release brief at `docs/specs/v0.7.0-network-ground-truth-and-environment-monitor.md` — anchors what's now in the canonical spec.

## 2. Optional polish (not blocking archive)
- [x] 2.1 README "Diagnostics" section gains a one-liner pointing to `openspec/specs/link-health/spec.md`.
- [x] 2.2 The DNS-detection / WAN-skip logic is comment-rich already; no doc work needed beyond the spec.
