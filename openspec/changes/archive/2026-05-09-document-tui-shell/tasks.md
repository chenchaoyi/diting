# Tasks — document tui-shell

## 1. Spec backfill — no implementation
- [x] 1.1 Spec extracted from `WifiScopeApp.compose`, the modal classes (HelpScreen, BasicsScreen, EventsScreen, BLEDetailScreen), `GroupedFooter`, and the `_refresh_environment_panel` view-routing in `src/wifiscope/tui.py`.
- [x] 1.2 Cross-checked panel-stability invariant against `tests/test_tui_smoke.py`.

## 2. Optional polish (not blocking archive)
- [x] 2.1 The TUI's docstring / class docstring of `WifiScopeApp` cross-links to the canonical spec.
- [x] 2.2 README "Layout" section gains a one-liner pointer.
