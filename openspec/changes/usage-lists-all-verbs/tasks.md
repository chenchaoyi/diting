# Tasks

- [x] `cli.py` `_usage`: add `capture` / `setup` / `update` to the subcommand
      list (in `_COMMANDS` order); correct the `--json` automation line
- [x] `test_cli.py`: guard test — every `_CANONICAL_VERBS` entry appears in
      `_usage()`
- [x] README (EN + ZH): list the current `--json`-capable verbs + the non-TUI
      subcommand set
- [x] `tests/TESTING.md` + `docs/zh/TESTING.md` row BEFORE the test
- [x] Run all four CI gates
